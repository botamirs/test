<!DOCTYPE html>
<html lang="en">
    <%- include("../include/admin/head") %> 
</head>
<body class="rtl">
    <%- include("../include/admin/header") %> 
    <div class="container-fluid">
        <div class="row">
            <!-- ---------------------------- -->
            <%- include("../include/admin/navbar") %>                 
            <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
                <% if (posts.length > 0) { %>
                    <h2 class="mt-2">آخرین پست ها</h2>
                    <div class="table-responsive">
                        <table class="table table-striped table-sm">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>عنوان</th>
                                    <th>تاریخ</th>
                                    <th>وضعیت</th>
                                    <th>تست</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for (let post of posts) { %> 
                                    <tr>
                                        <td>1</td>
                                        <td> <a href="/posts/<%= post._id %> "><%= post.title %></a>  </td>
                                        <td> <%= formatDate(post.createdAt) %> </td>
                                        <td>
                                            <span class="badge <%= post.status === "عمومی" ? 'badge-success' : 'badge-warning'%>">
                                                <%= post.status %> 
                                            </span>
                                        </td>
                                        <td>تست</td>
                                    </tr>
                                <% } %> 
                            </tbody>
                        </table>
                    </div>
                    <% } else { %>
                    <div class="text-center">
                        <h3 class="mt-2">
                            در حال حاضر هیچ پستی توسط شما در پایگاه داده ثبت نشده است
                        </h3>
                    </div>
                    <% } %> 
            </main>
        </div>
    </div>
    <%- include("../include/script") %> 
</body>
</html>
